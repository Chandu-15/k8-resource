apiVersion: v1
kind: Pod
metadata:
  name: secret-demo
  namespace: roboshop
  labels:
    project: roboshop
    component: frontend
spec:
  containers:
    - name: nginx
      image: nginx
      ports:
       - ContainerPort: 80
      envfrom:
        - SecretRef:
            name: pod-secret